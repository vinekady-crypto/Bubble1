<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="?android:attr/selectableItemBackground"
    android:gravity="center_vertical">

    <!-- Selection Checkbox (Hidden by default, visible on Long Press) -->
    <CheckBox
        android:id="@+id/checkbox_history"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:layout_marginEnd="10dp" />

    <!-- Icon -->
    <!-- FIX: Replaced 'ic_menu_text_opt' (missing) with 'ic_menu_edit' (public) -->
    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:src="@android:drawable/ic_menu_edit"
        android:tint="#757575"
        android:layout_marginEnd="12dp" />

    <!-- Text Info Container -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <!-- Preview of the scanned text -->
        <TextView
            android:id="@+id/text_history_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Scanned Text Preview..."
            android:textColor="#333333"
            android:textSize="15sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Timestamp -->
        <TextView
            android:id="@+id/text_history_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Just now"
            android:textColor="#9E9E9E"
            android:textSize="11sp"
            android:layout_marginTop="2dp" />
    </LinearLayout>

    <!-- Quick Copy Button -->
    <!-- FIX: Replaced 'ic_menu_copy' (private) with 'ic_menu_share' (public) -->
    <ImageButton
        android:id="@+id/btn_history_copy"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:src="@android:drawable/ic_menu_share"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:padding="6dp"
        android:tint="#757575"
        android:contentDescription="Copy Text" />

</LinearLayout>